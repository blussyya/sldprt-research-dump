# Evidence: v0.5 Parser/Viewer Validation

**Source script or method:** `v0.5/test/run-corpus-tests.js`, `v0.5/test/compare-with-reference.js`

**Exact commands:**
```
node v0.5/test/run-corpus-tests.js
node v0.5/test/compare-with-reference.js
```
(both wired to `npm test` in `v0.5/package.json`)

**Script paths:** `v0.5/test/run-corpus-tests.js`, `v0.5/test/compare-with-reference.js`, exercising `v0.5/src/parser-core.js` + `v0.5/src/node-cli.js`.

**Input files:** Same 8-file corpus as v0.4.5/v0.4.6:
- `test files original/usb hub case (ultimate test)/USB hub case BOTTOM.SLDPRT`
- `test files original/usb hub case (ultimate test)/USB hub case TOP.SLDPRT`
- `test files original/Helical Bevel Gear.SLDPRT`
- `test files original/Dekor.SLDPRT`
- `test files original/SW2000-s01.SLDPRT` (OLE2, expected rejection)
- `test files original/distributor main boss rev a.SLDPRT`
- `test files original/Pocket Wheel.SLDPRT`
- `test files original/PTC GE8080-8.SLDPRT`

**Reference data compared against:** `v0.4.5/EXP023_RESULTS_CORRECTED.json` (per-face), and the published aggregate numbers in `knowledge/evidence/2026-08-13_v0.4.5-EXP024-corrected.md` (category totals).

**Corpus size:** 7 files with usable DisplayLists (8 attempted). HEADPHONE not present in this repository checkout (same limitation as v0.4.5/v0.4.6).

**Faces/models tested:** 1,172 faces across 7 files.

---

## Raw output: `run-corpus-tests.js` (aggregate parity)

```
PASS: Total valid faces (1172)
PASS: Total candidates (4688)
PASS: Total INVALID_EC (2344)
PASS: Total INVALID_VC (1172)
PASS: INVALID_NORMALS removes 0 faces from the known-good corpus (0)
Per-file: BOTTOM 39, TOP 68, GEAR 113, DEKOR 375, DISTRIBUTOR 51, POCKET 400, PTC 126
  -- all match reference exactly.
Per-file INV016_FAIL / INV017_FAIL / INV018_FAIL: 0 / 0 / 0 for every file.
Per-file loopSize-sum-mismatch (independent re-check of INV-007): 0 for every file.
SW2000: detected as OLE2, clear unsupported-format error produced (not silently skipped).

ALL CHECKS PASSED (1172 faces, parity with v0.4.5/v0.4.6 confirmed).
```

## Raw output: `compare-with-reference.js` (per-face parity)

```
Loaded reference: 1172 faces from v0.4.5/EXP023_RESULTS_CORRECTED.json

BOTTOM: reference 39, v0.5 39
TOP: reference 68, v0.5 68
GEAR: reference 113, v0.5 113
DEKOR: reference 375, v0.5 375
DISTRIBUTOR: reference 51, v0.5 51
POCKET: reference 400, v0.5 400
PTC: reference 126, v0.5 126

Compared 1172 reference faces against v0.5 parser output.
EXACT MATCH: every (file, mp) -> {edgeCount, vertexCount, secCount} triple
agrees with v0.4.5/EXP023_RESULTS_CORRECTED.json.
```

0 mismatches across 1,172 faces on marker offset, edge count, vertex count, and section count.

---

## Browser verification (manual, headless Chromium via Playwright)

Not a corpus-scale automated test -- a functional smoke test of the actual UI against real files, performed before declaring the viewer functional, per the "start the dev server and use the feature in a browser" requirement for UI work.

Method: served `v0.5/web/` over local HTTP; CDN dependencies (pako, three.js, OrbitControls) substituted with locally-cached copies of the identical files only because this sandboxed environment's headless Chromium could not reach the CDN through the sandbox's proxy (confirmed via direct `curl` through the same proxy succeeding) -- an environment artifact of this session, not a change to the shipped `web/index.html`, which still references the CDN URLs.

**USB hub case BOTTOM.SLDPRT:**
- Parsed correctly: format=openswx, DisplayLists=98,481 bytes, total candidates=156, valid=39, rejected=117, triangles rendered=1,488, unrenderable loops=0.
- Face list populated with 39 entries; selecting face #0 populated the metadata panel correctly (marker=0x17a2, ec=4, vc=4, secCount=1, loopSizes=[4], triCount=2).
- Wireframe/vertices/normals/solid-mesh toggles all functioned without error.
- Screenshot confirms a plausible, recognizable rendered mesh with wireframe, vertex points, and normal lines all visible simultaneously.
- 0 console/page errors.

**Dekor.SLDPRT (larger, multi-loop faces present):**
- Parsed correctly: valid=375, rejected=1,125, triangles rendered=15,282, unrenderable loops=0.
- A `secCount=1044` face (mp=0x2636) selected via the face list; metadata panel showed 1,044 loop sizes and 3,774 rendered triangles.
- Canvas click-to-select (three.js raycasting -> triangle index -> face index mapping) confirmed working: after clicking the canvas, the metadata panel became visible and a face list entry was marked selected.
- Screenshot shows the rendered mesh for this file as a visually chaotic, clearly-implausible starburst pattern -- recorded as the observation behind new open question OQ-019 (see `knowledge/OPEN_QUESTIONS.md`), not treated as evidence of a parser bug (the underlying vertex/normal/offset data for this face independently matches the v0.4.5 reference exactly, per the per-face comparison above).
- 0 console/network errors.

**SW2000-s01.SLDPRT (OLE2, expected-unsupported):**
- Error box displayed the exact unsupported-format message; HUD updated to "Parse failed -- see errors in the sidebar". No silent failure, no crash.

---

**Related experiment:** v0.4.5 EXP-023-CORRECTED/EXP-024-CORRECTED (reference data), v0.4.6 EXP-026 (reference pipeline lineage).

**Related conclusion:** `v0.5/SUMMARY.md`, `v0.5/README.md`.

**Date produced:** 2026-08-13

**Date captured:** 2026-08-13

**Known gaps:**
- HEADPHONE (62 faces) untested -- file not present in this repository checkout, same as v0.4.5/v0.4.6.
- Browser verification used locally-cached copies of the CDN libraries due to a sandbox proxy limitation in this session; the shipped `web/index.html` is unmodified and still references the CDN URLs directly (same pattern as the pre-existing `v0.2.2/web/viewer.html`).
- The per-face comparison checks `edgeCount`/`vertexCount`/`secCount` only (not full vertex/normal float-by-float equality), since the v0.4.5 reference JSON does not store raw vertex/normal arrays. Vertex/normal correctness is instead guaranteed structurally: `parser-core.js` reads them at the same offsets established by INV-002, using the same gap/normals-unit-length gates as EXP-021/EXP-024-CORRECTED.
